defmodule ModuleName do
  def hello do
    IO.puts("Hello, World!")
  end
end

# Notice here how Elixir doesn't need to create a file only to create a new module; elixir modules can be defined directly in the shell
